---
报告ID: <唯一报告ID>
任务ID: <唯一任务ID>
主题: <任务主题>
日期: YYYY-MM-DD
最终状态: 放行
版本引用:
  仓库提交: <commit 或 null>
  工作区版本: <version 或 null>
评审状态: approved
评审记录引用:
  - <repo-backed reviewer verdict path>
证据引用:
  - <repo-backed evidence path>
替代报告: null
---

# <任务名> Completion Report

## 1. 任务摘要

| 字段 | 内容 |
| --- | --- |
| 任务 | `<任务名>` |
| 日期 | `YYYY-MM-DD` |
| 主要受众 | `生产放行 / 验收` |
| 最终状态 | `放行 / 条件放行 / 阻断 / 终止` |
| 版本引用 | `<commit / version pin>` |
| 评审结论 | `<评审状态 + reviewer verdict>` |
| 相关设计 / 规范 | `<spec / issue / PR>` |

## 2. 背景与目标

- **背景**：为什么开始这项工作。
- **目标**：这次任务的成功标准是什么。

## 3. 范围

### 包含范围

1. `<本次明确包含的工作>`
2. `<本次明确包含的工作>`

### 不包含范围

1. `<本次明确不处理的内容>`
2. `<本次明确不处理的内容>`

## 4. 实现与变更摘要

- `<关键改动点>`
- `<关键文件 / 脚本 / 配置变化>`
- `<行为层面的变化>`

## 5. 验证与门禁结果

| 验证项 | 结果 | 说明 |
| --- | --- | --- |
| `<命令 / gate / E2E>` | `通过 / 失败 / 条件通过` | `<关键说明>` |
| `<命令 / gate / E2E>` | `通过 / 失败 / 条件通过` | `<关键说明>` |

### Gate 矩阵

| Gate | 级别 | 状态 | 说明 |
| --- | --- | --- | --- |
| `<gate 名称>` | `必需 / 可选` | `pass / fail / blocked / skipped / not-run` | `<关键说明>` |
| `<gate 名称>` | `必需 / 可选` | `pass / fail / blocked / skipped / not-run` | `<关键说明>` |

## 6. 产物与状态变化

### 关键产物

- `<新增 / 更新的关键文件、目录或输出>`
- `<新增 / 更新的关键文件、目录或输出>`

### 状态变化

- **改前**：`<关键初始状态>`
- **改后**：`<关键最终状态>`

## 7. 发现的问题与修复

| 问题 | 影响 | 处理结果 |
| --- | --- | --- |
| `<bug / 设计缺口>` | `<影响面>` | `<已修复 / 延期 / 非阻断>` |
| `<bug / 设计缺口>` | `<影响面>` | `<已修复 / 延期 / 非阻断>` |

## 8. 残余风险与未完成项

- `<已知残余风险>`
- `<明确遗留事项>`
- `<条件放行时需要遵守的边界>`

## 9. Agent / 模型参与矩阵

| 角色 | Agent / 模型 | 职责 |
| --- | --- | --- |
| 主执行 | `<agent / model>` | `<职责>` |
| 独立审计 / reviewer | `<agent / model>` | `<职责>` |
| 其他参与者 | `<agent / model>` | `<职责>` |

## 10. 最终结论

- **结论**：`<放行 / 条件放行 / 阻断 / 终止>`
- **原因**：`<为什么给出这个结论>`
- **下一步**：`<最小必要的后续动作>`

## 11. 附录：证据索引

| 证据类型 | 路径 / 冻结摘要 |
| --- | --- |
| 关键代码 / 配置 | `<repo-backed path>` |
| 关键命令摘要 | `<已冻结到 DCR 或 evidence 的命令摘要>` |
| artifact / review / telemetry | `<artifact-backed path>` |
| 真实状态文件 | `<repo-backed / vault-backed path>` |
| reviewer verdict | `<repo-backed verdict path>` |

> 若结论依赖 session `plan.md`、SQL 状态或交互式输出，请先把关键事实冻结到本报告或 repo-backed evidence，再引用。  
> 若证据涉及私人 URL、书签标题或本地敏感数据，默认仅保留脱敏后的最小必要摘要。
